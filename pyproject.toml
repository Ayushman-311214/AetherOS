[project]
name = "aetheros"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ayushman-311214", email = "ayushman311214@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
]

[project.scripts]
aetheros = "aetheros:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
